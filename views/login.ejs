<section class="mx-auto max-w-xl rounded-lg bg-white p-8 shadow">
  <h1 class="mb-6 text-2xl font-semibold text-rstgray">Entrar</h1>
  <p class="mb-8 text-sm text-gray-600">
    Introduza o seu email para receber um código de acesso de 6 dígitos.
  </p>
  <form action="/login/send-otp" method="POST" class="space-y-6">
    <div>
      <label class="mb-2 block text-sm font-medium text-gray-700" for="email">Email</label>
      <input
        required
        type="email"
        id="email"
        name="email"
        value="<%= email || '' %>"
        class="w-full rounded border-gray-300 focus:border-rstred focus:ring-rstred"
      />
    </div>
    <button
      type="submit"
      class="w-full rounded bg-rstred px-4 py-3 text-center text-sm font-semibold uppercase tracking-wide text-white transition hover:opacity-90">
      Enviar código
    </button>
  </form>
</section>
