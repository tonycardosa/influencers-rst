<% if (flash) { %>
  <div
    class="mb-6 rounded border px-4 py-3 text-sm font-semibold <%= flash.type === 'error' ? 'border-red-200 bg-red-50 text-red-700' : 'border-green-200 bg-green-50 text-green-700' %>">
    <%= flash.message %>
  </div>
<% } %>
