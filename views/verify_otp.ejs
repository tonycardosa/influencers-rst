<section class="mx-auto max-w-xl rounded-lg bg-white p-8 shadow">
  <h1 class="mb-6 text-2xl font-semibold text-rstgray">Introduza o código</h1>
  <p class="mb-8 text-sm text-gray-600">
    Enviámos um OTP para <strong><%= email %></strong>. Verifique a caixa de entrada (ou spam).
  </p>
  <form action="/login/verify-otp" method="POST" class="space-y-6">
    <input type="hidden" name="email" value="<%= email %>" />
    <div>
      <label class="mb-2 block text-sm font-medium text-gray-700" for="otp_code">Código</label>
      <input
        required
        type="text"
        id="otp_code"
        name="otp_code"
        maxlength="6"
        pattern="\\d{6}"
        class="w-full rounded border-gray-300 tracking-[1rem] text-center text-lg font-semibold focus:border-rstred focus:ring-rstred"
      />
    </div>
    <button
      type="submit"
      class="w-full rounded bg-rstred px-4 py-3 text-center text-sm font-semibold uppercase tracking-wide text-white transition hover:opacity-90">
      Confirmar
    </button>
  </form>
</section>
